server {
    listen 80;
    listen [::]:80;
    server_name keonjhardigitallibrary.in;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/letsencrypt;
        default_type text/plain;
    }

    location / {
        return 302 http://lms.fyolicrafts.com$request_uri;
    }
}
