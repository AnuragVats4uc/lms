enum StudentStatus {
  ACTIVE
  INACTIVE
  BLOCKED
}

enum StudentRole {
  STUDENT
  ADMIN
  SUPER_ADMIN
  SUPPORT
}

enum StudentGender {
  MALE @map("Male")
  FEMALE @map("Female")
  OTHER @map("Other")
}
